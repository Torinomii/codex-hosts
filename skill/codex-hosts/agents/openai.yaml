interface:
  display_name: "Codex Hosts"
  short_description: "Use saved SSH and Telnet hosts safely"
  default_prompt: "Use $codex-hosts to run bounded single-host, multi-command, or multi-host work without exposing credentials, and reuse authenticated SSH sessions when possible."
