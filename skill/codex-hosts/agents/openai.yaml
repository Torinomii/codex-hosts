interface:
  display_name: "Codex Hosts"
  short_description: "Saved SSH/Telnet hosts and memory-only secrets"
  default_prompt: "Use $codex-hosts to run a command on a saved host, complete a host in its editor, or collect memory-only project secrets as field references."
