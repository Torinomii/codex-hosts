interface:
  display_name: "Codex Hosts"
  short_description: "Manage hosts and memory-only secret references"
  default_prompt: "Use $codex-hosts for bounded SSH/Telnet work or memory-only project secrets: collect missing values in the in-app editor, reuse ready field references, and request explicit approval for each secret-consuming execution without reading plaintext."
